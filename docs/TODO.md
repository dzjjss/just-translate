# TODO（基准 v0.17.5；当前 v0.17.7）

按预计工程量从小到大；会话丢译文和仅译文显示错误仍为 P0。

- [x] v0.17.7：维护区分为阅读导出、缓存清理和问题诊断；精简说明、日志详情折叠、统一间距，缓存数量独立展示。
- [x] 全局即时操作反馈：面板、悬浮球、HUD；处理/成功/失败/部分成功、有界等待、防重复触发、可见结果提示（v0.17.7 改为不占布局空间的浮动提示）、输入变化后不采用旧回执、即时设置保存反馈。
- [x] v0.17.7：面板、HUD、悬浮菜单的小字与对比度修整；保持纸色和大标题宋体；译文取消重复透明度衰减，保留现有单层轻阴影与高对比模式回退。
- [x] v0.17.7：导出按当前页面顺序排列，图注/表题不再冒充章节，保留列表编号与段落归属、表格行列；排除明确站点 UI，转义 Markdown/HTML 特殊字符，验证真实下载。
- [ ] P0：追查会话失效后整页译文消失；新增保留译文设置，检查重连/重扫/清理链路及标题变化导致清空的问题。
- [ ] P0：仅译文模式成功后才隐藏对应原文，失败保留原文；修复列表、表格、链接和裸文本混排及切回恢复。
- [ ] 双语兼容：单字一行、镜像/倒置、Flex/Grid、窄容器、宿主/祖先变换；覆盖用户两张截图场景并保留交互。
- [ ] 缓存优先模式：尽早复用已有结果和预检缓存；在语义匹配前提下改进整页全命中、分块整批绑定限制，显示命中数量。

DS Flash 模型失败调查已移除；用户已确认 Pro 可用。v0.17.6 的 UI 等待超时不等于后台已取消，不自动重发请求。
